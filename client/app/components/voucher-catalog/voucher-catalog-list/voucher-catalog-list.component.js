import controller from './voucher-catalog-list.controller';
import template from './voucher-catalog-list.html';
import './voucher-catalog-list.less';

const voucherCatalogListComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller,
    controllerAs: 'vm'
};

export default voucherCatalogListComponent;