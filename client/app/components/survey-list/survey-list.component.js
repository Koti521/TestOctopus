import './survey-list.less';
import template from './survey-list.html';
import controller from './survey-list.controller';

const surveyListComponent = {
    template,
    controller,
    controllerAs: 'vm'
};

export default surveyListComponent;